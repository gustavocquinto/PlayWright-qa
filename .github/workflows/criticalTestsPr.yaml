name: Playwright PR Tests

on:
  pull_request:
    branches:
      - master

jobs:
  test:
    runs-on: ubuntu-latest
    env:
        TEST_BASE_URL: https://front.serverest.dev/login
        TEST_USER_LOGIN: gutocross.dx@gmail.com
        TEST_USER_PASSWORD: Senha@Forte123
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: 22.20.0

      - name: Install dependencies
        run: npm ci

      - name: Install playwright
        run: npx playwright install

      - name: Run Playwright critical tests
        run: npx playwright test --grep "P1"